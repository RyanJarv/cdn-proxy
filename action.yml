# action.yml
name: 'GitHub Action Proxy'
description: 'Setups up a proxy to send requests from GitHub actions.'
#inputs:
#  who-to-greet:  # id of input
#    description: 'Who to greet'
#    required: true
#    default: 'World'
#outputs:
#  time: # id of output
#    description: 'The time we greeted you'
runs:
  using: 'docker'
  image: 'github/Dockerfile'
#  args:
#    - ${{ inputs.who-to-greet }}
